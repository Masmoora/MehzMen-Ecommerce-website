<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account - MehzMen</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f5f5f5;
        }
        .signup-container {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }
        .form-title {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 20px;
        }
        .google-btn {
            border: 1px solid #ddd;
            padding: 10px;
            border-radius: 6px;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            background: white;
            cursor: pointer;
        }
        .google-btn img {
            width: 20px;
        }
        .right-img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>

<body>

    <div class="container py-5">
        <div class="row signup-container">

            <!-- LEFT SIDE – FORM -->
            <div class="col-md-6 p-5">

                <h2 class="form-title">Create Account</h2>

                <form action="/signup" method="POST">

                    <label>Your name</label>
                    <input type="text" name="name" class="form-control mb-3" placeholder="Label (required)" required>

                    <label>Email</label>
                    <input type="email" name="email" class="form-control mb-3" placeholder="Label (required)" required>

                    <label>Mobile Number</label>
                    <input type="tel" name="phone" class="form-control mb-3" placeholder="Label (required)" required>

                    <label>Password</label>
                    <input type="password" name="password" class="form-control mb-3" placeholder="Label (required)" required>

                    <label>Confirm Password</label>
                    <input type="password" name="confirmPassword" class="form-control mb-4" placeholder="Label (required)" required>

                    <!-- Referral Section -->
                    <p class="text-center mt-2 mb-1">Anyone has referred you? Claim the reward</p>
                    <div class="d-flex justify-content-center">
                        <button class="btn btn-outline-dark mb-3">REFERRAL CODE</button>
                    </div>

                    <p class="text-center">
                        Already have an account?  
                        <a href="/login">Log in</a>
                    </p>

                    <!-- Signup Button -->
                    <button type="submit" class="btn btn-dark w-100 py-2 mt-2">SignUp</button>

                    <p class="text-center mt-3">Or Register with</p>

                    <!-- Google Button -->
                    <div class="google-btn mt-2">
                        <img src="/images/google.png" alt="Google">
                        <span>Google</span>
                    </div>

                </form>
            </div>

            <!-- RIGHT SIDE – IMAGE -->
            <div class="col-md-6 p-0 d-none d-md-block">
                <img src="/images/signup-man.jpg" class="right-img">
            </div>

        </div>
    </div>

</body>
</html>
