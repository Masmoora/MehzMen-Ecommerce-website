<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Error Page | Zyra</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #fdf8f6;
      font-family: 'Nanum Myeongjo', serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      flex-direction: column;
      text-align: center;
      padding: 20px;
    }
    .error-icon {
      font-size: 80px;
      color: #e74c3c;
      margin-bottom: 20px;
    }
    .error-title {
      font-size: 32px;
      font-weight: 700;
      color: #7d1a1a;
      margin-bottom: 10px;
    }
    .error-code {
      color: #e74c3c;
      font-weight: 600;
      font-size: 20px;
      margin-bottom: 15px;
    }
    .error-message {
      color: #555;
      font-size: 18px;
      margin-bottom: 25px;
    }
    .btn-home {
      background-color: #823d2b;
      color: white;
      border: none;
      padding: 12px 25px;
      border-radius: 8px;
      font-weight: 500;
      text-decoration: none;
    }
    .btn-home:hover {
      background-color: #7d1a1a;
      color: white;
    }

    @media (max-width: 576px) {
      .error-icon {
        font-size: 60px;
      }
      .error-title {
        font-size: 26px;
      }
      .error-message {
        font-size: 16px;
      }
      .btn-home {
        padding: 10px 20px;
        font-size: 14px;
      }
    }
  </style>
</head>
<body>

  <div class="error-icon">
    <i class="bi bi-exclamation-triangle-fill"></i>
  </div>

  <h1 class="error-title">Oops!</h1>
  <h5 class="error-code">Error 404 - Page not found</h5>
  <p class="error-message">The page you are trying to access is not available. You may go back to the admin dashboard.</p>

  <a href="/admin/dashboard" class="btn-home">Back to Dashboard</a>

</body>
</html>